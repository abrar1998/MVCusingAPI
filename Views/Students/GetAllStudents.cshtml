@model IEnumerable<Using_Api_In_MVC.Models.Student>

<p>
    <a asp-action="Create">Create New</a>
</p>

@{
    if(TempData["data"]!=null)
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            <strong>Success!</strong> @TempData["data"].
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }
    else if (TempData["data1"]!=null)
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            <strong>Edited!</strong> @TempData["data1"].
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }
    else if(TempData["data2"]!=null)
    {
        <div class="alert alert-warning alert-dismissible fade show" role="alert">
            <strong>Deleted!</strong> @TempData["data2"].
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

}

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.email)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.email)
            </td>
            <td>
                    <button class="btn btn-secondary">
                        @Html.ActionLink("Edit", "Edit", new {  id=item.id })
                    </button>
                    <button class="btn btn-secondary">
                        @Html.ActionLink("Details", "Details", new { id = item.id })
                    </button>
                    <button class="btn btn-secondary">
                        @Html.ActionLink("Delete", "Delete", new { id = item.id })
                    </button>
                    @*  <a asp-action="Edit" asp-controller="Students" asp-rout-id="new{ @item.id}" class="btn btn-secondary"><button>Edit</button></a> |
                    <a asp-action="Details" asp-controller="Students" asp-rout-id="@item.id" class="btn btn-secondary"><button>Details</button></a> |
                    <a asp-action="Delete" asp-controller="Students" asp-rout-id="@item.id" class="btn btn-secondary"><button>Delete</button></a> *@
               @*  @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ }) *@
               
            </td>
        </tr>
}
    </tbody>
</table>
